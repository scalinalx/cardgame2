# The repo ships the Full-HD optimized imgs/ (~66MB). The 4K print masters and the
# optimizer's scratch output stay LOCAL. Re-optimize: bash tools/optimize-images.sh ; node tools/genimages.js
imgs_print/
imgs_web/

# Local editor / tooling config (allowlists etc.)
.claude/

# OS / editor junk
.DS_Store
*.log
Thumbs.db

# Node (none currently — hygiene)
node_modules/
